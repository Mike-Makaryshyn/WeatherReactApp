import React from 'react';
import cl from './MonthStatistics.module.scss';

interface Props {
   
}

const MonthStatistics = (props:Props) => {
   return (
      <div className={cl.monthStatistics}>
         MonthStatistics
      </div>
   );
};

export default MonthStatistics;